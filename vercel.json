{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "functions": {
    "api/**/*.ts": {
      "maxDuration": 60
    }
  },
  "rewrites": [
    { "source": "/api/churches/:path*", "destination": "/api/churches?path=:path*" },
    { "source": "/:path((?!api/).*)", "destination": "/index.html" }
  ]
}
